# Imports in Python.

import math
from math import pi  # Can import one variable or function also.
from math import sqrt as sq

print(sq(2))
print(pi)

# This will tell me all the variables and functions present in imported library.
print(dir(math))
